spring:
  datasource:
    url: jdbc:h2:mem:seqdiagram;MODE=Oracle;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  sql:
    init:
      mode: always
      schema-locations: classpath:test-schema.sql

app:
  db:
    auto-init: false
  ai:
    base-url: http://localhost:1/v1
